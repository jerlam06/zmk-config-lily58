# OLED display
CONFIG_ZMK_DISPLAY=y

# Optional but recommended for Studio convenience —
# skips the unlock step over USB. Comment out if you want
# Studio to require unlock for security.
CONFIG_ZMK_STUDIO_LOCKING=n
